in: query
name: user[hauptperson_attributes][buchungen_attributes][0][schwimmlevel]
required: true
description: >-
  Schwimmlevel des Reisenden. Die möglichen Werte können aus den geschlossenen
  API Endpunkt für die Destination entnommen werden (data -> attributes ->
  schwimmlevel -> optionen).
schema:
  type: string
  example: Seepferdchen (Frühschwimmer)
